# 初回実装 タスクリスト

## 変更履歴
- 2026-02-25: 初版作成

---

## 進捗サマリー

| フェーズ | 状態 | 完了日 |
|---------|------|--------|
| Phase 0: 環境セットアップ | ✅ 完了 | 2026-02-25 |
| Phase 1: マップ・移動システム | ✅ 完了 | 2026-02-25 |
| Phase 2: 物件・資産システム | ✅ 完了 | 2026-02-25 |
| Phase 3: カードシステム | ✅ 完了 | 2026-02-25 |
| Phase 4: 目的地・ボンビーシステム | ✅ 完了 | 2026-02-25 |
| Phase 5: イベントシステム | ✅ 完了 | 2026-02-25 |
| Phase 6: CPU（AI）システム | ✅ 完了 | 2026-02-25 |
| Phase 7: セーブ・ロードシステム | ✅ 完了 | 2026-02-25 |
| Phase 8: 演出・UI仕上げ | ✅ 完了 | 2026-02-25 |
| Phase 9: テスト・品質チェック | ✅ 完了 | 2026-02-25 |
| Phase 10: GameScene統合・ゲームプレイ完成 | ✅ 完了 | 2026-02-25 |
| Phase 11: カード効果拡充・アップグレードUI | ✅ 完了 | 2026-02-25 |
| Phase 12: カード売り場・残カード効果実装 | ✅ 完了 | 2026-02-25 |
| Phase 13: buy_property・double_income・ResultScene充実 | ✅ 完了 | 2026-02-25 |
| Phase 14: プレイヤー選択UI（bombee_transfer / card_steal） | ✅ 完了 | 2026-02-26 |
| Phase 15: get_money/pay_money 高度効果修正・CPU売り場対応 | ✅ 完了 | 2026-02-26 |
| Phase 16: sell_property 全targetType対応・物件選択UI | ✅ 完了 | 2026-02-26 |
| Phase 17: 残カード効果対応・都市選択UI・サイコロ倍率・ボンビー免疫 | ✅ 完了 | 2026-02-26 |
| Phase 18: card_steal/selected_card 全カード選択UI・2段階ダイアログ | ✅ 完了 | 2026-02-26 |
| Phase 19: CPUカード使用・後退移動・マップマーカー | ✅ 完了 | 2026-02-26 |
| Phase 20: 物件所有ドット・CPUアクショントースト | ✅ 完了 | 2026-02-26 |
| Phase 21: 破産処理・地代支払い時の物件強制売却 | ✅ 完了 | 2026-02-26 |
| Phase 22: テスト拡充・負債表示・formatManEn改善 | ✅ 完了 | 2026-02-26 |
| Phase 23: ターン開始バナー・CPUカード購入トースト | ✅ 完了 | 2026-02-26 |
| Phase 24: EventManagerバグ修正・カード捨て機能 | ✅ 完了 | 2026-02-26 |
| Phase 25: 都市クリック物件情報ポップアップ | ✅ 完了 | 2026-02-26 |
| Phase 26: ゲームスタートバナー・手札上限通知 | ✅ 完了 | 2026-02-26 |
| Phase 27: 独占達成通知・CPUアップグレードトースト | ✅ 完了 | 2026-02-26 |
| Phase 28: プレイヤーパネル改善（ランキング表示・現在プレイヤー強調） | ✅ 完了 | 2026-02-26 |
| Phase 29: コードレビュー指摘修正（バグ修正6件） | ✅ 完了 | 2026-02-26 |

> ⬜ 未着手 / 🔄 進行中 / ✅ 完了

---

## Phase 0: 環境セットアップ

- [x] Vite + TypeScript プロジェクト初期化
- [x] Phaser.js インストール
- [x] Vitest インストール・設定
- [x] ESLint・Prettier インストール・設定
- [x] ディレクトリ構造の初期化
- [x] `src/config.ts` 作成（定数・カラー・アニメーション時間）
- [x] `src/main.ts` 作成（Phaser.Game インスタンス生成）
- [x] ブラウザで起動確認（npm run build 成功・lint エラーなし）

✅ Phase 0 完了 (2026-02-25)

---

## Phase 1: マップ・移動システム

- [x] `public/assets/data/cities.json` 作成（56都市）
- [x] `public/assets/data/routes.json` 作成（84路線）
- [x] `src/types/game.ts` 作成
- [x] `src/types/board.ts` 作成
- [x] `src/types/card.ts` 作成
- [x] `src/types/event.ts` 作成
- [x] `src/types/save.ts` 作成
- [x] `src/types/index.ts` 作成
- [x] `src/game/BoardManager.ts` 作成
- [x] `src/utils/mapUtils.ts` 作成（緯度経度→キャンバス座標変換）
- [x] `src/utils/format.ts` 作成（万円フォーマット）
- [x] `src/utils/random.ts` 作成（サイコロ・ランダム選択）
- [x] `src/scenes/BootScene.ts` 作成
- [x] `src/scenes/PreloadScene.ts` 更新（cities/routes JSON読み込み）
- [x] `src/scenes/PlayerSetupScene.ts` 実装（プレイヤー設定UI）
- [x] `src/scenes/GameScene.ts` 実装（マップ描画・サイコロ・コマ移動）
- [x] ビルド成功・型エラーなし

✅ Phase 1 完了 (2026-02-25)

---

## Phase 2: 物件・資産システム

- [x] `public/assets/data/properties.json` 作成（109件・43都市）
- [x] `src/game/PropertyManager.ts` 作成
- [x] 物件購入オーバーレイ実装（OverlayScene）
- [x] 年末決算オーバーレイ実装（OverlayScene）
- [x] 目的地ボーナス・地代支払いオーバーレイ実装
- [x] GameScene と PropertyManager・OverlayScene の連携
- [x] ビルド成功・型エラーなし・lintクリーン

✅ Phase 2 完了 (2026-02-25)

---

## Phase 3: カードシステム

- [x] `public/assets/data/cards.json` 作成（50枚・14種類）
- [x] `src/types/card.ts` 更新（CardType拡張・CardRarity追加）
- [x] `src/game/CardManager.ts` 作成
- [x] OverlayScene にカード関連オーバーレイを組み込み済み（GameSceneから呼び出し）
- [x] ビルド・型エラーなし

✅ Phase 3 完了 (2026-02-25)

---

## Phase 4: 目的地・ボンビーシステム

- [x] `src/game/DestinationManager.ts` 作成
- [x] `src/game/BombeeManager.ts` 作成（ミニ/ノーマル/キング3段階）
- [x] 目的地到着ボーナスオーバーレイ（OverlayScene）
- [x] ビルド・型エラーなし

✅ Phase 4 完了 (2026-02-25)

---

## Phase 5: イベントシステム

- [x] `public/assets/data/events.json` 作成（39件・月次/年次/ランダム）
- [x] `src/types/event.ts` 更新（EventType・EventTrigger追加）
- [x] `src/game/EventManager.ts` 作成
- [x] PreloadSceneでevents.jsonロード
- [x] ビルド・型エラーなし

✅ Phase 5 完了 (2026-02-25)

---

## Phase 6: CPU（AI）システム

- [x] `src/game/CpuManager.ts` 作成
  - [x] やさしい：ランダム行動
  - [x] ふつう：基本戦略（目的地優先・ROI判断）
  - [x] むずかしい：最適戦略（BFS距離・独占評価・カードスコアリング）
- [x] ビルド・型エラーなし

✅ Phase 6 完了 (2026-02-25)

---

## Phase 7: セーブ・ロードシステム

- [x] `src/types/save.ts` 確認（既存）
- [x] `src/save/ISaveManager.ts` 作成
- [x] `src/save/LocalStorageSaveManager.ts` 作成
- [x] TitleScene にセーブスロット選択UI追加（3スロット）
- [x] ビルド・型エラーなし

✅ Phase 7 完了 (2026-02-25)

---

## Phase 8: 演出・UI仕上げ

- [x] サイコロアニメーション（ランダム数字ちらつき演出）
- [x] コマ移動tweenアニメーション
- [x] OverlayScene（物件購入・年末決算・目的地・イベント）
- [x] ResultScene 完成（順位表示・優勝者強調・フェードイン）
- [x] TitleScene にセーブスロットUI追加
- [x] ゲーム終了後ResultSceneへ遷移

✅ Phase 8 完了 (2026-02-25)

---

## Phase 9: テスト・品質チェック

- [x] `tests/game/PropertyManager.test.ts`（17テスト）
- [x] `tests/game/CardManager.test.ts`（10テスト）
- [x] `tests/game/BombeeManager.test.ts`（12テスト）
- [x] `tests/utils/random.test.ts`（7テスト）
- [x] `npm run lint` でエラーなし
- [x] `npm run test` で全テスト通過（46/46）
- [x] `randomPickN` バグ修正（ループ条件評価の誤り）
- [x] ビルド成功（型エラーなし）

✅ Phase 9 完了 (2026-02-25)

---

## Phase 10: GameScene 統合・ゲームプレイ完成

- [x] コマラベルバグ修正（`pawns: Arc[]` → `pawnContainers: Container[]`でコマ＋番号を一体化）
- [x] ステップ移動実装（`movePawnStepByStep`で1都市ずつ再帰移動）
- [x] 分岐選択ダイアログ（人間プレイヤーの路線選択UI）
- [x] BombeeManager 統合（init時に最下位配置・endTurnでボンビー行動）
- [x] EventManager 統合（月替わりイベント抽選・順次表示）
- [x] CpuManager 統合（物件購入・路線選択を難易度別AI判断）
- [x] DestinationManager 統合（正式ボーナス計算・新目的地設定）
- [x] カードマスでカードドロー（1移動につき1枚・`cardDrawnThisMove`フラグ）
- [x] 手札カードUI（`show_card_hand` オーバーレイ・使うボタン）
- [x] teleportPawn（`move_to_destination`カード使用時のコマテレポート）
- [x] セーブボタンをゲーム画面に追加（スロット1固定）
- [x] GameState のイミュータブル更新徹底（spread演算子）
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 10 完了 (2026-02-25)

---

## Phase 11: カード効果拡充・物件アップグレードUI

- [x] `CardManager.ts` に `move_to_city` 実装（`effect.targetType` の都市IDへ位置更新）
- [x] `CardManager.ts` に `bombee_away` 実装（`bombeeType: 'none'` にリセット）
- [x] `CardManager.ts` に `sell_property` 実装（最初の所有物件を `value`% で売却・totalAssets再計算）
- [x] `CardManager.ts` に `move_steps`・`plus_dice` のスイッチケース追加（手札削除のみ、効果はGameScene側）
- [x] `GameScene.ts` に `extraDiceBonus` フィールド追加（plus_diceカード用）
- [x] `GameScene.rollAndMove()` を `extraDiceBonus` 対応に更新
- [x] `GameScene.handleCardUse()` に `move_to_city`・`move_steps`・`plus_dice` ハンドリング追加
- [x] `GameScene.checkPropertyAndEndTurn()` に自分の物件アップグレード選択を追加（CPU常時UG）
- [x] `OverlayScene.ts` に `PropertyUpgradeData` インターフェース追加
- [x] `OverlayScene.ts` に `showPropertyUpgrade()` メソッド追加（Lv/費用/収益増分表示）
- [x] `GameScene.showCardDrawToast()` 追加（カード入手時のフェードイン/アウトトースト）
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 11 完了 (2026-02-25)

---

## Phase 12: カード売り場・残カード効果実装

- [x] `config.ts` に `SHOP_CITY_IDS` 追加（札幌・仙台・東京・名古屋・大阪・広島・福岡・那覇ほか10都市）
- [x] `CardManager.ts` に `steal_property` 実装
  - `any_player`: 最富プレイヤーの最安値物件を奪う
  - `current_city_all`: 現在地の全対戦相手物件を奪う（GameSceneが `targetCityId` を渡す）
- [x] `CardManager.ts` に `monopoly_break` 実装
  - 独占中の都市があれば最安値物件を80%返金で市場に戻す
  - 独占がなければ独占に最も近い相手の物件を1つ市場に戻す
- [x] `CardManager.ts` に `recalcTotalAssetsLocal()` プライベートヘルパー追加（PropertyManager非依存）
- [x] `OverlayScene.ts` に `CardShopData` インターフェース追加
- [x] `OverlayScene.ts` に `showCardShop()` メソッド追加（レアリティ表示・価格・購入ボタン）
- [x] `GameScene.ts` に `SHOP_CITY_IDS` インポート追加
- [x] `GameScene.onPlayerArrived()` → `checkShopAndProperty()` に分割
- [x] `GameScene.openCardShop()` メソッド追加（購入後に手札が空なら再オープン）
- [x] `GameScene.handleCardUse()` に `targetCityId: player.position.cityId` を渡すよう更新
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 12 完了 (2026-02-25)

---

## Phase 13: buy_property・double_income カード実装・ResultScene充実

- [x] `types/game.ts` に `Player.incomeMultiplier?: number` 追加（double_incomeカード用）
- [x] `CardManager.ts` に `buy_property` 実装
  - `current_location_discount`: 現在地の最安値未所有物件を割引購入（value% OFF）
  - `any_location`: 全国最安値の未所有物件を通常価格で購入
  - `current_city_free_one`: 現在地の未所有物件を1件無料取得
- [x] `CardManager.ts` に `double_income` 実装
  - `Player.incomeMultiplier` を card.effect.value に設定
- [x] `PropertyManager.calcYearEndIncome()` に `incomeMultiplier` 適用
  - 各プレイヤーの `totalIncome × multiplier` を適用し、決算後に `incomeMultiplier` を1にリセット
- [x] `GameScene.createInitialState()` に `incomeMultiplier: 1` 追加
- [x] `ResultScene.showRankings()` に物件数表示を追加（「所持金 XXX  物件N件」形式）
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 13 完了 (2026-02-25)

---

## Phase 14: プレイヤー選択UI（bombee_transfer / card_steal）

- [x] `OverlayScene.ts` に `PlayerSelectData` インターフェース追加
- [x] `OverlayScene.ts` に `show_player_select` イベントリスナー追加
- [x] `OverlayScene.ts` に `showPlayerSelect()` メソッド追加
  - 対象プレイヤー一覧（名前・総資産）を表示
  - 「選択」ボタンで選択、「キャンセル」でキャンセル
- [x] `GameScene.ts` の `handleCardUse()` を `handleCardUse` + `applyCardEffect` に分割
  - `handleCardUse`: bombee_transfer / card_steal カードはプレイヤー選択ダイアログを先に表示
  - `applyCardEffect`: カード効果を実際に適用（旧handleCardUseのロジックをそのまま移動）
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 14 完了 (2026-02-26)

---

## Phase 15: get_money/pay_money 高度効果修正・CPU売り場対応

- [x] `CardManager.ts` の `useGetMoney()` を `targetType` 対応に更新
  - `flat`: 従来どおり固定額取得
  - `property_value_percent`: 所有物件総額 × value% を取得
  - `all_players_each`: 全対戦相手から value 万円ずつ徴収（相手の所持金上限）
- [x] `CardManager.ts` の `usePayMoney()` を `targetType` 対応に更新
  - `flat`: 従来どおり固定額支払い
  - `property_value_percent`: 所有物件総額 × value% を支払い（修繕費）
  - `total_assets_percent`: 総資産 × value% を支払い（税金）
  - `all_players_each`: 全対戦相手に value 万円ずつ支払い
- [x] `GameScene.ts` に `cpuVisitShop()` メソッド追加
  - 所持金が価格の3倍以上あれば最大2枚購入
- [x] `GameScene.checkShopAndProperty()` でCPUも売り場を訪問するよう更新
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 15 完了 (2026-02-26)

---

## Phase 16: sell_property 全targetType対応・物件選択UI

- [x] `OverlayScene.ts` に `PropertySelectData` インターフェース追加
- [x] `OverlayScene.ts` に `show_property_select` イベントリスナー追加
- [x] `OverlayScene.ts` に `showPropertySelect()` メソッド追加
  - 売却可能物件一覧（物件名・購入価格・収益）を表示
  - 「売る」ボタンで売却、「キャンセル」でキャンセル
- [x] `CardManager.ts` の `useSellProperty()` を全targetType対応に書き換え
  - `current_city_all`: 現在地の自分所有物件をすべて売却（`value`% 返金）
  - `opponent_forced`: 対象プレイヤーの最安値物件を強制売却（代金は対象プレイヤーへ）
  - `self_choice` (デフォルト): `targetPropertyId` 指定物件を売却（未指定なら最初の所有物件）
- [x] `GameScene.ts` の `handleCardUse()` に `sell_property` ハンドリング追加
  - `self_choice`: 物件選択ダイアログを表示してから `applyCardEffect` 呼び出し
  - `opponent_forced`: プレイヤー選択ダイアログを表示してから `applyCardEffect` 呼び出し
- [x] `applyCardEffect()` のoptions型に `targetPropertyId` を追加
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 16 完了 (2026-02-26)

---

## Phase 17: 残カード効果対応・都市選択UI・サイコロ倍率・ボンビー免疫

- [x] `types/game.ts` に `diceMultiplier?: number`・`bombeeImmuneYears?: number` を追加
- [x] `CardManager.ts` の `useBombeeAway()` を `self_permanent` 対応に更新
  - `self_permanent`: ボンビー除去 + `bombeeImmuneYears = card.effect.value`（デフォルト1）
- [x] `CardManager.ts` に `useBombeeTransferLastToSecond()` を追加
  - 最下位プレイヤーのボンビーを2位最下位へ自動移動（UIなし）
- [x] `CardManager.ts` の `useMoveToDest()` に `double_move_next_turn` 対応を追加
  - 次ターンの `diceMultiplier` を card.effect.value（デフォルト2）に設定
- [x] `CardManager.ts` の `useDoubleIncome()` に `one_month` 対応を追加
  - 現在月の収益を即時 value 倍で付与（年末決算を待たない）
- [x] `OverlayScene.ts` に `CitySelectData` インターフェース追加
- [x] `OverlayScene.ts` に `show_city_select` イベントリスナー追加
- [x] `OverlayScene.ts` に `showCitySelect()` メソッド追加（3列グリッドレイアウト）
- [x] `GameScene.ts` の `handleCardUse()` に `move_steps/any_station` テレポート対応追加
  - 都市選択ダイアログを表示し、選択後に対象都市へテレポート
- [x] `GameScene.ts` の `handleCardUse()` に `bombee_transfer/last_to_second` 対応追加
  - プレイヤー選択UIなしで自動実行
- [x] `GameScene.ts` の `applyCardEffect()` に `any_station` テレポート処理追加
- [x] `GameScene.ts` の `rollAndMove()` に `diceMultiplier` 適用（使用後リセット）
- [x] `GameScene.ts` の年末決算処理に `decrementBombeeImmunity()` 呼び出し追加
- [x] `GameScene.ts` の `createInitialState()` に `diceMultiplier: 1`・`bombeeImmuneYears: 0` 追加
- [x] `BombeeManager.ts` の `attachBombeeToLastPlace()` に免疫プレイヤースキップ対応追加
- [x] `BombeeManager.ts` の `maybeMoveToLastPlace()` に免疫プレイヤースキップ対応追加
- [x] `BombeeManager.ts` に `decrementBombeeImmunity()` を追加
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 17 完了 (2026-02-26)

---

## Phase 18: card_steal/selected_card カード選択UI・2段階ダイアログ

- [x] `CardManager.ts` の `UseCardOptions` に `targetCardId?: string` を追加
- [x] `CardManager.ts` の `useCardSteal()` に `targetCardId?` パラメータを追加
  - `targetCardId` が指定されておりターゲットの手札にある場合はそのカードを奪取
  - 未指定ならランダムに奪取（random_card との互換性維持）
- [x] `OverlayScene.ts` に `CardSelectData` インターフェース追加
- [x] `OverlayScene.ts` に `show_card_select` イベントリスナー追加
- [x] `OverlayScene.ts` に `showCardSelect()` メソッド追加
  - 対象プレイヤーの手札を一覧表示（カード名・説明・レアリティ）
  - 「奪う」ボタンで選択、「キャンセル」でキャンセル
- [x] `GameScene.ts` の `handleCardUse()` で `card_steal/selected_card` を2段階フローに更新
  - Step1: プレイヤー選択ダイアログ
  - Step2: 選択プレイヤーの手札からカード選択ダイアログ
  - Step3: `applyCardEffect()` に `targetPlayerId` + `targetCardId` を渡す
- [x] `GameScene.ts` の `applyCardEffect()` の options 型に `targetCardId` を追加
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 18 完了 (2026-02-26)

---

## Phase 19: CPUカード使用・後退移動・マップマーカー

- [x] `GameScene.ts` に `cpuTakeTurn()` メソッド追加
  - `decideCardUse()` でカード使用判断 → 使用後サイコロ or スキップしてサイコロ
  - 対象プレイヤーが必要なカードは総資産最多の対戦相手を自動選択
  - 移動系カード使用時はサイコロを二重に振らないよう制御
- [x] `nextPlayer()` の CPU 自動ターン処理を `cpuTakeTurn()` に変更
- [x] `types/game.ts` の `Position` に `previousCityId?: string` を追加
- [x] `GameScene.ts` の `moveOneStep()` で `previousCityId` を記録
- [x] `GameScene.ts` に `movePawnBackward()` メソッド追加
  - `previousCityId` 方向を優先して後退、なければランダムな隣接都市へ
- [x] `applyCardEffect()` の `move_steps` 処理で `steps < 0` の場合に `movePawnBackward()` を使用
- [x] `GameScene.ts` にマップマーカーフィールド追加（`destinationMarker`, `bombeeMarker`）
- [x] `GameScene.ts` に `updateMapMarkers()` メソッド追加
  - 目的地マーカー（黄色★）：`destinationCityId` の都市上に表示
  - ボンビーマーカー（赤丸＋アイコン）：ボンビー憑依プレイヤーの現在地に表示
- [x] `updateTopBar()` から `updateMapMarkers()` を呼び出し（状態変化のたびに更新）
- [x] `create()` で初期マーカーを描画
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 19 完了 (2026-02-26)

---

## Phase 20: 物件所有ドット・CPUアクショントースト

- [x] `GameScene.ts` に `propertyDotContainers: Phaser.GameObjects.Container[]` フィールド追加
- [x] `GameScene.ts` の `updateMapMarkers()` に物件所有ドット描画を追加
  - 都市ごとに所有者のプレイヤーカラーで小さなドットを並べて表示
  - `propertyDotContainers` を毎回クリア＆再描画
- [x] `GameScene.ts` に `showActionToast()` メソッド追加
  - 画面中央に暗い背景＋テキストのトーストを表示
  - フェードイン→一定時間表示→フェードアウトのアニメーション
- [x] `GameScene.ts` の `cpuTakeTurn()` でカード使用時に `showActionToast()` を呼び出し
  - 「{CPU名} が「{カード名}」を使用！」とトーストで通知
  - 600ms 遅延後にカード効果を適用
- [x] `updatePlayerPanel()` から `updateMapMarkers()` を呼び出し（毎ターン更新）
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 20 完了 (2026-02-26)

---

## Phase 21: 破産処理・地代支払い時の物件強制売却

- [x] `PropertyManager.ts` に `sellPropertyForcibly()` を追加
  - 購入価格の50%で物件を売却（アップグレード状態を問わない）
  - 物件は市場に戻る（ownerId: null, upgradeLevel: 0）
- [x] `OverlayScene.ts` に `BankruptcyData` インターフェース追加
- [x] `OverlayScene.ts` に `show_bankruptcy` イベントリスナー追加
- [x] `OverlayScene.ts` に `showBankruptcy()` メソッド追加
  - オレンジ枠のダイアログで所持金不足を通知
  - 不足額・所有物件一覧（半額売却ボタン付き）を表示
  - 物件なし時: 「続ける（負債確定）」ボタンのみ
  - 「このまま続ける（負債）」ボタンで負債を受け入れる
- [x] `GameScene.ts` の `checkShopAndProperty()` の地代支払い分岐を更新
  - `player.money < fee` の場合、`handleBankruptcy()` を経由してから支払い
  - 支払い後は全額支払い（負債でマイナスになる場合も許容）
- [x] `GameScene.ts` に `handleBankruptcy()` メソッド追加
  - 人間: `show_bankruptcy` ダイアログを表示、売却後に再チェック
  - CPU: 最安値物件を自動売却、解消するまで再帰
  - 物件0件または負債受け入れ: `onResolved()` コールバックを呼び出して続行
- [x] `GameScene.ts` の import に `BankruptcyData` を追加
- [x] ビルド成功・型エラーなし・全46テスト通過

✅ Phase 21 完了 (2026-02-26)

---

## Phase 22: テスト拡充・負債表示・formatManEn改善

- [x] `tests/game/PropertyManager.test.ts` に `sellPropertyForcibly` テストを追加（4テスト）
  - 購入価格50%で売却・物件が市場に戻ること
  - アップグレード済みでも元値50%で売却されること
  - 他人の物件は売却できないこと
  - 総資産が再計算されること
- [x] `tests/game/BombeeManager.test.ts` に `decrementBombeeImmunity` テストを追加（3テスト）
  - 免疫年数が1以上のプレイヤーを1年減らすこと
  - 免疫年数0はそのまま
  - 免疫年数1は0になること
- [x] `tests/game/BombeeManager.test.ts` に `attachBombeeToLastPlace（免疫）` テストを追加（1テスト）
  - 最下位プレイヤーが免疫中なら次の最下位に憑依すること
- [x] `src/utils/format.ts` の `formatManEn()` をマイナス値対応に更新
  - `-5000` → `-5,000万円`
  - `-15000` → `-1.5億円`
- [x] `GameScene.ts` の `updatePlayerPanel()` で所持金がマイナスなら赤テキストで表示
- [x] lint クリーン（0 errors, 0 warnings）
- [x] ビルド成功・型エラーなし・全54テスト通過（46 → 54）

✅ Phase 22 完了 (2026-02-26)

---

## Phase 23: ターン開始バナー・CPUカード購入トースト

- [x] `GameScene.ts` に `showTurnBanner()` メソッド追加
  - 画面中央に「{プレイヤー名} のターン」を大きく表示
  - 紺色（0x1a2a4a）半透明背景 + 白太字テキスト（depth: 100/101）
  - フェードイン（300ms）→ 保持（900ms）→ フェードアウト（300ms）
  - フェードアウト後に `onComplete()` コールバックを呼び出し
- [x] `GameScene.ts` の `nextPlayer()` を `showTurnBanner()` 対応に更新
  - バナー表示中はサイコロボタンを無効化 (`setDiceButtonEnabled(false)`)
  - バナー完了後、人間: サイコロ有効化 / CPU: `cpuTakeTurn()` 呼び出し
- [x] `GameScene.ts` の `cpuVisitShop()` でカード購入時にトースト表示
  - 1枚購入: `{CPU名} が「{カード名}」を購入！`
  - 複数枚購入: `{CPU名} がカードをN枚購入！`
- [x] ビルド成功・型エラーなし・全54テスト通過

✅ Phase 23 完了 (2026-02-26)

---

## Phase 24: EventManagerバグ修正・カード捨て機能

- [x] `EventManager.ts` の `applyMoneyPercent()` のマイナス所持金バグ修正
  - マイナス所持金プレイヤーにペナルティを与えない（ボーナスは通常通り適用）
  - 修正前: `Math.max(0, p.money + delta)` → 負債プレイヤーが0万円に戻ってしまう
  - 修正後: delta < 0 のとき `p.money > 0 ? Math.max(-p.money, delta) : 0`
- [x] `EventManager.ts` の `applyMoneyFixed()` のマイナス所持金バグ修正
  - 修正前: `Math.max(delta, -p.money)` → p.money が負なら delta を正にしてしまう
  - 修正後: `Math.max(delta, -Math.max(0, p.money))` → 負債時はペナルティ 0
- [x] `EventManager.ts` の `applyAllMoneyEqualizer()` 同修正
- [x] `OverlayScene.ts` の `CardHandData` に `onDiscardCard` コールバック追加
- [x] `OverlayScene.ts` の `showCardHand()` にカード捨てボタン（灰色「捨」）追加
  - 「使う」ボタンを cx+155 に移動、「捨」ボタンを cx+230 に配置
- [x] `GameScene.ts` の `onCardButtonClick()` に `onDiscardCard` ハンドラ追加
  - `cardManager.discardCard()` を呼び出し（既存メソッドを再利用）
  - 捨て後に `showActionToast()` で「{カード名} を捨てました」通知
- [x] `CardManager.ts` の重複 `discardCard` 定義を削除（既存実装を活用）
- [x] ビルド成功・型エラーなし・全54テスト通過

✅ Phase 24 完了 (2026-02-26)

---

## Phase 25: 都市クリック物件情報ポップアップ

- [x] `OverlayScene.ts` に `CityPropertyInfo` インターフェース追加
  - `property`: 物件データ、`ownerName`: 所有者名（未所有なら null）、`ownerColor`: コマ色（null）
- [x] `OverlayScene.ts` に `CityInfoData` インターフェース追加
  - `cityName`, `regionName`, `properties: CityPropertyInfo[]`, `onClose`
- [x] `OverlayScene.ts` に `show_city_info` イベントリスナー追加
- [x] `OverlayScene.ts` に `showCityInfo()` メソッド追加
  - 都市名・地域名をタイトル帯に表示
  - 物件一覧（所有者カラードット・物件名・価格・収益・Lvを表示）
  - 物件なし時: 「この都市に物件はありません」
  - 背景クリック or「閉じる」ボタンで閉じる
- [x] `GameScene.ts` の `drawMap()` に透明ヒットゾーン追加
  - 各都市の中心に 22×22 の透明 Rectangle を配置
  - `pointerdown` で `showCityInfoPopup(cityId)` を呼び出し
- [x] `GameScene.ts` に `showCityInfoPopup()` メソッド追加
  - `BoardManager` から都市データ取得
  - `PropertyManager` から現在の物件状態を参照
  - 地域名の日本語マッピング（8地域）
  - `show_city_info` イベントを OverlayScene に emit
- [x] `GameScene.ts` の import に `CityInfoData` 追加
- [x] ビルド成功・型エラーなし・全54テスト通過

✅ Phase 25 完了 (2026-02-26)

---

## Phase 26: ゲームスタートバナー・手札上限通知

- [x] `GameScene.ts` に `GAME_CONFIG` インポート追加
- [x] `GameScene.ts` に `isNewGame` フラグ追加（新規ゲーム判定）
- [x] `GameScene.ts` の `init()` で `isNewGame = !data.gameState` を設定
- [x] `GameScene.ts` の `create()` 末尾を修正
  - ダイスボタンを無効化（`setDiceButtonEnabled(false)`）
  - 400ms 後に `startFirstTurn()` 呼び出し
- [x] `GameScene.ts` に `startFirstTurn()` メソッド追加
  - 新規ゲームなら `showStartBanner()` → ターンバナー
  - ロードゲームならターンバナーのみ
  - CPU の場合は thinking delay → `cpuTakeTurn()`
- [x] `GameScene.ts` に `showStartBanner()` メソッド追加
  - 赤背景（#8b0000）・金色テキスト「ゲームスタート！」
  - 白テキスト「目標：N年後に総資産トップを目指せ！」
  - フェードイン400ms → 保持1400ms → フェードアウト350ms → onComplete()
- [x] `GameScene.ts` のカードマス処理を修正
  - `canDrawCard` が false の場合に人間プレイヤーへ `showHandFullToast()` を呼び出し
- [x] `GameScene.ts` に `showHandFullToast()` メソッド追加
  - グレー背景、「手札が一杯！（8/8枚）\nカードマスを通過しました」
  - `showCardDrawToast()` と同じアニメーション挙動
- [x] ビルド成功・lint クリーン・全54テスト通過

✅ Phase 26 完了 (2026-02-26)

---

## Phase 27: 独占達成通知・CPUアップグレードトースト

- [x] `PropertyManager.ts` に `hasMonopoly(properties, cityId, playerId)` メソッド追加
  - 都市の全物件が playerId に所有されている場合 true
- [x] `GameScene.ts` の CPU 物件購入後に独占チェック追加
  - `hasMonopoly()` が true なら `showActionToast(「〇〇が△△市を独占！」)` を表示
- [x] `GameScene.ts` の人間プレイヤー物件購入後に独占チェック追加
  - `hasMonopoly()` が true なら `showMonopolyToast(cityName)` を表示
- [x] `GameScene.ts` に `showMonopolyToast()` メソッド追加
  - 深紅背景（#8b0000）・金色テキスト「★ 〇〇を独占！\n収益が2倍になります！」
  - `depth: 110`（最前面）、フェードイン250ms → 保持1200ms → フェードアウト350ms
- [x] `GameScene.ts` の CPU アップグレード処理にトースト追加
  - 「〇〇が「△△」をLvNにアップグレード！」
- [x] `tests/game/PropertyManager.test.ts` に `hasMonopoly` テスト4件追加
  - 全物件保有で true / 他人の物件混在で false / 都市に物件なしで false / 未所有あれば false
- [x] ビルド成功・lint クリーン・全58テスト通過

✅ Phase 27 完了 (2026-02-26)

---

## Phase 28: プレイヤーパネル改善（ランキング表示・現在プレイヤー強調）

**問題**: `createPlayerPanel()` で設定した現在プレイヤーのボーダーが、ターン切り替え時に更新されていなかった。またランキング表示がなかった。

- [x] `GameScene.ts` に `playerPanelBgs: Phaser.GameObjects.Rectangle[]` フィールド追加
- [x] `createPlayerPanel()` でボーダーの初期設定を削除し、`playerPanelBgs` に保存
- [x] `updatePlayerPanel()` を改善
  - 総資産順でランクを算出（`rankMap: Map<string, number>`）
  - 各プレイヤーの名前テキスト（index 0）を `${rank}位 ${player.name}` に更新
  - 現在プレイヤーには `▶` プレフィックスを付加
  - `playerPanelBgs[i]` の枠線をターン切り替えのたびに更新（現在プレイヤー: 2px stroke / 非アクティブ: stroke なし）
- [x] ビルド成功・lint クリーン・全58テスト通過

✅ Phase 28 完了 (2026-02-26)

---

## Phase 29: コードレビュー指摘修正（バグ修正6件）

サブエージェントによるコードレビューで発見された🔴必須修正事項6件を対応。

- [x] **CPU破産の再帰 → ループ変換** (`GameScene.ts`)
  - 再帰で `ownedProperties` 参照が蓄積してスタックが積まれるリスクを解消
  - `while` ループで最新の `this.gameState.properties` を毎回取得
- [x] **カード奪取の手札上限チェック** (`CardManager.ts`)
  - `useCardSteal()` で奪取者の手札が満杯の場合は `{ success: false, reason: 'hand_full' }` を返す
  - `UseCardResult.reason` 型に `'hand_full'` を追加
- [x] **ボンビー移転のボンビーなし状態ガード** (`CardManager.ts`)
  - `useBombeeTransfer()` で `from.bombeeType === 'none'` の場合は移転不可
- [x] **openCardShop の古い player 参照修正** (`GameScene.ts`)
  - 再帰呼び出しで常に `this.gameState.players[playerIndex]` から最新の player を取得するよう明示
- [x] **OverlayScene リスナー二重登録対策** (`OverlayScene.ts`)
  - `create()` 冒頭で `this.events.removeAllListeners()` を呼びシーン再起動時の重複登録を防止
- [x] **overlayScene ゲッターでキャスト一元化** (`GameScene.ts`)
  - `private get overlayScene(): OverlayScene` ゲッターを追加
  - 散在していた `this.scene.get(SCENE_KEYS.OVERLAY) as OverlayScene`（19箇所）を `this.overlayScene` に一元化
- [x] ビルド成功・lint クリーン・全58テスト通過

✅ Phase 29 完了 (2026-02-26)

